name: NPM Build

env:
  DOCKER_DIRECTORY: "${{ github.workspace }}"
  DOCKER_CONTAINER: "neuroviewer-app"
  DOCKER_IMAGE: "nginx:stable-alpine"

on:
  repository_dispatch:
    types: [test_workflow]
  push:
    branches: [ main, development ]
  pull_request:
    branches: [ main, development ]

jobs:

  package_neuroconnect:
      runs-on: self-hosted
      if: github.ref == 'refs/heads/main'
      steps:
        - uses: actions/checkout@v2
        - name: Package neuroconnect
          if: always()
          run: |
            npm install
            CI=false npm run build

        - name: scp build files to main host
          uses: appleboy/scp-action@master
          if: always()
          with:
            host: ${{ secrets.MAIN_HOST }}
            username: ${{ secrets.USERNAME }}
            password: ${{ secrets.PASSWORD }}
            source: "dist,Dockerfile,default.conf"            
            target: "/home/mcpadmin/temp/neuroviewer"
            rm: true

  deploy_neuroconnect:
    runs-on: self-hosted
    needs: package_neuroconnect
    if: github.ref == 'refs/heads/main'
    steps:
      - name: executing remote ssh commands to relaunch Docker and deploy to main server
        uses: appleboy/ssh-action@master
        if: always()
        with:
          host: ${{ secrets.MAIN_HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          script: |
            sudo docker container stop ${{ env.DOCKER_CONTAINER }}
            sudo docker container rm ${{ env.DOCKER_CONTAINER }}
            sudo docker system prune -a -f
            sudo docker build -t ${{ env.DOCKER_IMAGE }} /home/mcpadmin/temp/neuroviewer/
            # sudo docker run -dit --name ${{ env.DOCKER_CONTAINER }} -p 3000:80 -e TZ=America/Los_Angeles -v /src/connectivity-plot:/app --restart=always ${{ env.DOCKER_IMAGE }}
            # sudo docker container ls -a

  slack:
    runs-on: ubuntu-latest
    needs: deploy_neuroconnect
    steps:
      - uses: craftech-io/slack-action@v1
        with:
          slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
        if: always()