version: '3.7'

networks:
  local:

services:
    connectivity:
        container_name: neuroviewer-dev-app
        build:
                context: .
                dockerfile: Dockerfile
        ports:
        - 3603:3603